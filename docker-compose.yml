version: '3'

services:
  drone:
    image: drone/drone:2
    ports:
      - 80:80
    volumes:
      - drone-data:/data
    restart: always
    environment:
      - DRONE_GITHUB_CLIENT_ID=Ov23liLjGepr2UUFJCkF
      - DRONE_GITHUB_CLIENT_SECRET=d869157689bd87c9d7115d2e4a73b7892cf953da
      - DRONE_RPC_SECRET=supersecret
      - DRONE_SERVER_HOST=localhost
      - DRONE_SERVER_PROTO=http

  runner:
    image: drone/drone-runner-docker:1
    ports:
      - 3000:3000
    restart: always
    depends_on:
      - drone
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - DRONE_RPC_PROTO=http
      - DRONE_RPC_HOST=drone
      - DRONE_RPC_SECRET=supersecret
      - DRONE_RUNNER_CAPACITY=2
      - DRONE_RUNNER_NAME=my-runner

volumes:
  drone-data:
